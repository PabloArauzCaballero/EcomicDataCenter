# Registro de hallazgos de hardening

## Convenciones

- **Severidad** combina impacto técnico, exposición y probabilidad.
- **Resuelto** significa corregido en la rama y respaldado por los gates de CI.
- **Mitigado** significa que el riesgo fue reducido, pero conserva trabajo residual.
- **Bloqueante** impide aprobar una entrega productiva.

## Hallazgos

| ID | Severidad | Área | Hallazgo | Estado | Evidencia o corrección |
|---|---|---|---|---|---|
| HD-001 | Crítica | Despliegue | `docker-compose.yml` contenía servicios duplicados, dependencias repetidas y una topología de migración inválida. | Resuelto | Compose reconstruido con migrador de una sola ejecución, API dependiente de migraciones, red interna y validación en CI. |
| HD-002 | Alta | Alcance | El repositorio mezclaba el núcleo económico con módulos heredados de otros productos, ampliando análisis, compilación y superficie de revisión. | Mitigado | Alcance explícito en TypeScript, Jest, ESLint, Prettier y validadores; detalle en `legacy-quarantine.md`. Extracción física queda recomendada. |
| HD-003 | Alta | Recursos | HTTP y conexión inicial a PostgreSQL carecían de límites explícitos en la configuración de aplicación. | Resuelto | Timeouts validados para request, conexión HTTP, keep-alive y conexión PostgreSQL. |
| HD-004 | Alta | Seguridad | Los errores inesperados podían registrar mensajes crudos de infraestructura, incluyendo SQL o valores sensibles proporcionados por dependencias. | Resuelto | Metadatos seguros de error, eliminación del mensaje crudo y pruebas de no exposición. |
| HD-005 | Alta | Seguridad | El `x-request-id` externo no estaba normalizado, lo que permitía valores excesivos o caracteres de control en correlación y logs. | Resuelto | Allowlist de caracteres, máximo de 128 y UUID generado por servidor para entradas inválidas. |
| HD-006 | Alta | Supply chain | No existía análisis CodeQL ni actualización automática de dependencias. | Resuelto | CodeQL fijado por SHA y Dependabot para npm y GitHub Actions. |
| HD-007 | Alta | Dependencias | La auditoría de dependencias debía confirmar que el candidato no contenía vulnerabilidades altas o críticas conocidas. | Resuelto | `yarn security:audit` terminó correctamente en el workflow final; el gate de release continúa siendo bloqueante ante futuras regresiones. |
| HD-008 | Media | Configuración | Swagger podía habilitarse accidentalmente en producción. | Resuelto | Validación de entorno rechaza `SWAGGER_ENABLED=true` en producción. |
| HD-009 | Media | Consultas | La paginación por offset aceptaba páginas arbitrariamente profundas, generando trabajo excesivo en PostgreSQL. | Mitigado | `pageSize <= 200` y `page <= 10000`; cursor estable queda como evolución si el consumo real lo exige. |
| HD-010 | Media | Recursos | No había evidencia ejecutada de estabilidad prolongada de memoria y conexiones. | **Bloqueante** | No se confirmó fuga estática; se exige soak test con RSS, heap, event-loop lag y pools. |
| HD-011 | Media | Continuidad | Existen scripts y runbooks de backup, pero no evidencia firmada de restauración aislada ni medición de RTO/RPO. | **Bloqueante** | Ejecutar restore drill en entorno aislado y conservar artefacto. |
| HD-012 | Media | Operación | Los límites de CPU y memoria dependen de la plataforma real y no están demostrados en este repositorio. | Pendiente de entorno | Configurar requests/limits o equivalentes en la plataforma de despliegue y validar bajo carga. |
| HD-013 | Media | Nomenclatura | No existía una comprobación automática de identificadores en inglés. | Resuelto | Gate AST `quality:naming` sobre el alcance productivo. |
| HD-014 | Media | Estándares | La documentación podía confundirse con una afirmación de certificación o nivel de disponibilidad físico. | Resuelto documentalmente | Matriz de estándares separa controles de software, organización e infraestructura física; prohíbe afirmar Tier, Rated o certificación sin auditoría. |
| HD-015 | Alta | Recursos | Un fallo del reader durante el arranque podía dejar abierto el pool writer antes de completar la aplicación Nest. | Resuelto | Writer y reader se crean y autentican como un grupo atómico; cualquier fallo cierra todos los pools creados y tiene prueba unitaria. |
| HD-016 | Media | Experiencia local | La guía indicaba ejecutar migraciones antes de iniciar PostgreSQL y el `.env.example` usaba el hostname Docker incluso para `yarn dev` en el host. | Resuelto | Generador de `.env` con modos Docker/host, override local de PostgreSQL y guía de verificación reproducible. |
| HD-017 | Media | Repositorio | Permanecían workflows y reportes temporales de diagnóstico dentro de la rama. | Resuelto | Se eliminaron workflows de una sola ejecución, reportes temporales, bytecode Python y caches rastreadas; `.gitignore` evita recurrencia. |
| HD-018 | Alta | Observabilidad | La ruta Pino `res.headers.set-cookie` era inválida para `fast-redact` y podía impedir el arranque real del proceso. | Resuelto | Se cambió a notación segura `res.headers["set-cookie"]`; el bootstrap de metadatos y el build de imagen lo verifican. |
| HD-019 | Media | Contratos | La exportación OpenAPI abría pools PostgreSQL y ocultaba errores de bootstrap, acoplando un artefacto determinista a infraestructura externa. | Resuelto | Exportador basado en `TestingModule`, conexiones dobles sin sockets, errores preservados y gate de CI con diagnóstico descargable ante fallos. |
| HD-020 | Media | Verificación local | La documentación local no estaba respaldada por una ejecución automática end-to-end del mismo flujo. | Resuelto | CI levanta Compose completo, ejecuta migraciones, seeds, health/readiness y smoke tests por NGINX, y siempre destruye volúmenes temporales. |
| HD-021 | Alta | Arranque local | Swagger estaba habilitado en desarrollo, pero faltaba `@fastify/static`, dependencia requerida por el adapter Fastify para servir sus recursos; el API entraba en reinicio continuo. | Resuelto | Se añadió `@fastify/static` compatible con Fastify 5, se actualizó el lockfile y el flujo Docker completo queda como gate de regresión. |
| HD-022 | Media | Calidad | El gate `format:check` fallaba en 250 archivos porque el repositorio no declaraba politica de fin de linea: verde en CI, rojo en todo checkout Windows. | Resuelto | `.gitattributes` con `eol=lf`, arbol normalizado a cero archivos CRLF y generadores Python emitiendo LF explicito. |
| HD-023 | Media | Seguridad (BOLA/BFLA) | `completeAgentRun` no verificaba aislamiento institucional: un agente con el UUID de un run ajeno podía cerrarlo y sobrescribir su checkpoint. | Resuelto | `review.service.ts` carga el agente del run y aplica la misma guarda de organización que `submission.service.ts`; el actor se propaga desde el controller. Confirmación de concurrencia pendiente de `test:integration`. |
| HD-024 | Baja | Seguridad (lectura) | `getAgentRun` devolvía métricas de cualquier run por ID sin scoping por organización para `INGESTION_AGENT`. | Resuelto | `agent-registry.service.ts#status` reutiliza `assertActorOrganization`; los roles custodios no escopados conservan visibilidad cruzada por política de divulgación. |
| HD-025 | Alta | Integridad/idempotencia | Ventana in-flight del import por lote: un `batchCode` existente pero incompleto no verificaba fingerprint, permitiendo procesar un payload distinto bajo la provenance del primero. | Resuelto | `batch-import.service.ts` exige `assertBatchFingerprint` al reclamar un batch existente; preserva el resume con mismo payload. +2 unit tests en `batch-idempotency.spec.ts`. Ejecutabilidad concurrente pendiente de `test:integration`. |
| HD-026 | Baja-Media | Contrato de error | Los `create` de provenance (organización/fuente/artefacto) eran check-then-create sin traducir la violación de unicidad: una carrera devolvía 500 en vez de 409/EXISTING. | Resuelto | `provenance.service.ts` traduce `UniqueConstraintError` a `ConflictError` (org/fuente) y a retorno idempotente `EXISTING` (artefacto). |
| HD-027 | Media | Observabilidad | El `x-request-id` se registraba y se incrustaba en errores, pero nunca se devolvía al cliente en respuestas exitosas: imposible mapear un reporte externo al log. | Resuelto | `request-context.interceptor.ts` fija `x-request-id` en toda respuesta. |
| HD-028 | Media | Observabilidad (USE) | No existían métricas de saturación de los pools reader/writer; la exhaustión de conexiones —estancamiento más probable— era invisible hasta manifestarse como latencia. | Resuelto | Gauge `observatory_database_pool_connections{role,state}` muestreado por `DatabasePoolMetricsCollector` (lectura en memoria, sin round-trip). |
| HD-029 | Baja | Observabilidad | Los gauges de dominio reportaban 0 falso durante ~60s tras cada arranque (sin `collect()` inicial). | Resuelto | `domain-metrics.collector.ts` ejecuta una recolección inmediata en `onModuleInit`. |
| HD-030 | Baja | Observabilidad/redacción | El query-string podía registrarse sin redactar y los comodines de redacción cubrían un solo nivel de anidamiento. | Resuelto | Se registra solo el path (sin query) en filtro y serializer Pino; se añadieron patrones `*.*.password`/`*.*.token`. |
| HD-031 | Media | Rendimiento | La consulta de datos ejecutaba dos subqueries `COUNT` sobre `quality_assessment` por fila que diferían solo en `status='FAIL'`. | Resuelto | Colapsadas a un único escaneo con `COUNT(*) FILTER`, preservando la forma de salida. Ejecutabilidad SQL pendiente de `test:integration`. |
| HD-032 | Alta | Rendimiento | La submission de intelligence procesa el lote completo (≤200) en una sola transacción serializable; un conflicto 40001 reintenta 200 ítems. | Diferido con propuesta | Requiere baseline de soak (HD-010) antes de fragmentar por bloques; `rule 50-performance` prohíbe optimizar sin medición. Ver `improvement-plan-2026-07.md`. |
| HD-033 | Media | Rendimiento | Re-fetch por registro de datos constantes del lote (estructura de dataset + reglas activas) en el bucle de ingesta. | Diferido con propuesta | Cambia un hot-path sin cobertura unitaria; requiere medición e integración sobre base desechable. Ver `improvement-plan-2026-07.md`. |
| HD-034 | Media | Ciclo de vida | El resultado `REQUEUED` de `reprocess` deja el ítem en `RECEIVED`, estado que ningún consumidor avanza (colas diferidas); `sweepAbandoned` acaba dead-leteándolo con motivo engañoso. | Decidido en ADR | Decisión registrada en **ADR-0014**: persistir el claim estructurado y re-ejecutar `reprocess` de forma síncrona (sin cola). Implementación = feature + migración; requiere evidencia de integración. |
| HD-035 | Alta/Media | Rendimiento | El bucle de ingesta por lote re-cargaba datos batch-constantes por registro (estructura de dataset publicada + reglas activas): ~2000+ consultas redundantes por lote de 500. | Resuelto | Caché perezosa `batch-registration-cache.ts` carga estructura y reglas una vez por lote y las reutiliza entre chunks; preserva la semántica de error por registro (con caché vacía carga igual que antes). Confirmación de statements/lote pendiente de `test:integration`. |
| HD-032/033 | Alta | Rendimiento | (ver arriba) HD-033 queda **resuelto** por HD-035; HD-032 (fragmentar submission) permanece no aplicado por romper el conteo de agregados del run en fallo parcial. | Parcial | HD-035 cierra el re-fetch (HD-033). HD-032 requiere idempotencia de replay a nivel submission: decisión en **ADR-0013** (feature + migración). |
| HD-036 | Baja | Clean-code | El ítem `REJECTED` de submission devolvía un sentinela mágico `rawObservationId:'0'` (bigint falso) que un cliente podía confundir con un ID real. | Resuelto | `rawObservationId` es opcional en `SubmissionItemResult` y se omite para `REJECTED`; sin drift de contrato (el campo no está en el response schema, solo como path param). |
| HD-038 | Alta | Supply chain | Advisory nuevo (2026-07) en `fast-uri` (transitiva de fastify vía `fast-json-stringify` y `@fastify/ajv-compiler`): `security:audit` reportaba 8 High. | Resuelto | `fast-uri` actualizado a 3.1.4 (rama 3.x, ajv) y 4.1.1 (rama 4.x, fast-json-stringify) por camino vulnerable, respetando los rangos de cada consumidor (`^3.0.0`/`^4.0.0`); `security:audit` → 0 vulnerabilidades; frozen-lockfile, typecheck, build y arranque real verificados. |
| HD-037 | Alta | Observabilidad/grants | **Descubierto al ejecutar smoke contra PostgreSQL real:** el `DomainMetricsCollector` corre por el pool reader y su query lee `intelligence.raw_observation`, pero la 0025 aísla esa zona del reader → la recolección fallaba con `permission denied` en **cada** ciclo, dejando los 5 gauges de dominio sin poblar en todo entorno correctamente migrado. | Resuelto | Migración aditiva y reversible `0029` con GRANT **a nivel de columna** `SELECT (processing_status, received_at)` al reader; el `payload_json` no confiable sigue inaccesible. Verificado end-to-end: 0 fallos del collector al arranque, gauges `dead_letter_items`/`ingestion_lag_seconds`/`open_contradictions` poblados, reversibilidad down/up y aislamiento de payload confirmados contra base local. |
| HD-039 | Alta | Correctitud de consulta | La paginación por cursor **descendente** comparaba la clave de orden como tupla (`(period_start, series_key) < …`), lo que implica `series_key DESC`, mientras la página se emite con `ORDER BY period_start DESC, series_key ASC`. Con empates en `period_start` —el caso normal: muchas series comparten periodo— la página siguiente re-servía filas ya entregadas y omitía en silencio el resto del grupo. | Resuelto | `data-query.plan.ts` usa el predicado que refleja el `ORDER BY` real (`period_start <` ó `period_start =` **y** `series_key >`); el orden emitido no cambia. +2 pruebas en `data-query.plan.spec.ts` que fijan ambas direcciones. |
| HD-040 | Media | Observabilidad/integridad | Cinco contadores se incrementaban **dentro** del callback de `withSerializableRetry`: un conflicto 40001 reejecuta el callback (hasta 3 intentos) y la métrica se contaba una vez por intento; un intento que terminaba en rollback reportaba un resultado que nunca ocurrió. | Resuelto | `review.service.ts` (×2), `reprocessing.service.ts` (×2) y `batch-import.service.ts#finalize` emiten tras el commit, alineados con el patrón que ya usaban `submission.service.ts` y `observation-registration.service.ts`. |
| HD-041 | Media | Seguridad (AuthN) | `jsonwebtoken` sólo comprueba `exp` cuando la reclamación está presente: un token emitido sin expiración se aceptaba indefinidamente, fuera del alcance de cualquier ventana de rotación o revocación. Sin `clockTolerance` declarada. | Resuelto | `jwt-auth.guard.ts` exige `exp` numérica además de `sub`, y declara `clockTolerance` de 5 s para el desfase con el emisor. |
| HD-042 | Media | Seguridad (AuthZ) | Guarda de aislamiento institucional *fail-open*: `if (actor.organizationId && agent && …)` omitía la comparación cuando no se hallaba el agente del run. La FK `fk_agent_run_ai_agent_id` (`ON DELETE RESTRICT`) garantiza que existe, de modo que la rama sólo podía desactivar la autorización en silencio. | Resuelto | `IntelligenceWriteRepository#requireAgentById` falla con `NOT_FOUND` en vez de resolver a `undefined`; aplicado en `submission.submit`, `review.completeRun` y `agent-registry.status`. |
| HD-043 | Media | Contrato de error | El pipe de validación lanzaba `BadRequestException` con las `issues` en el cuerpo, pero el filtro aplana un `HttpException` a su mensaje: el cliente recibía `"Invalid request"` sin saber qué campo falló. La rama `ZodError` del filtro era inalcanzable desde HTTP. | Resuelto | `RequestValidationError` transporta las issues por `details` (forma ya permitida por `ErrorResponse`, sin deriva de contrato); `toSafeValidationIssues` proyecta sólo `{code, path, message}` para no devolver el valor rechazado. +3 pruebas de pipe y +4 de filtro. |
| HD-044 | Media | Observabilidad | Los errores de plugin Fastify se respondían por `statusCode` sin registrar nada: un 5xx originado en un plugin no dejaba rastro en los logs y la única evidencia era el código que vio el cliente. | Resuelto | `http-exception.filter.ts` registra los 5xx de plugin (`Plugin request error`) manteniendo los 4xx sin ruido; la redacción del query-string se comparte con la rama no controlada. |
| HD-045 | Baja-Media | Arquitectura/rendimiento | `getAgentRun` (GET) abría una transacción SERIALIZABLE con reintento sobre el pool **writer** para una lectura pura; una flota de agentes consultando su propio progreso compite por conexiones de escritura con la ingesta que reportan. | Resuelto | `AgentRunQueryRepository` sirve el estado por `ReadQueryExecutor` (pool reader, transacción `READ ONLY`); la 0025 ya concede `SELECT` sobre `agent_run` y `ai_agent` al reader. `deadLetters` permanece en el writer **a propósito**: la 0025/0029 dejan sus columnas fuera del alcance del reader. |
| HD-046 | Baja | Clean-code/seguridad | `assertActorOrganization` estaba duplicado literalmente en cinco lugares; una copia divergente es indistinguible de una comprobación ausente. | Resuelto | Regla única en `common/auth/organization-scope.ts`, consumida por ingesta e inteligencia. |

## Conclusión técnica

La rama elimina defectos estructurales y reduce de forma material la superficie de ataque, el consumo no acotado y la fricción de validación local. Los gates automáticos cubren instalación reproducible, auditoría de dependencias, formato, lint, tipado, validadores, pruebas, migraciones, privilegios, seeds, build, imagen, contratos, CodeQL y el flujo Docker local completo.

No se declara lista para desplegar mientras permanezcan abiertos HD-010 y HD-011, y mientras HD-012 no se cierre en la plataforma real. Esos puntos requieren evidencia operativa y del entorno, no otra corrección documental.

La revisión dimensional de julio 2026 (`improvement-plan-2026-07.md`) cerró nueve hallazgos
adicionales (HD-023…HD-031): dos de autorización (aislamiento institucional en cierre y lectura de
runs), uno de idempotencia de lote (ventana in-flight), uno de contrato de error en provenance, tres
de observabilidad (correlation-id devuelto, saturación de pools, gauges sin ventana ciega) y uno de
rendimiento de consulta. Verificación ejecutada: typecheck, build, format, lint, 176 pruebas
unitarias y `quality:all` 17/17. HD-032…HD-034 quedan diferidos con propuesta por requerir baseline
de soak (rendimiento) o una decisión de arquitectura (ADR-0003). Las garantías que viven en la base
(concurrencia de HD-023/HD-025, ejecutabilidad SQL de HD-031) exigen `yarn test:integration` sobre una
base desechable para su confirmación final.

La revisión integral del 30 de julio de 2026 (`improvement-plan-2026-07-30.md`) cerró ocho hallazgos
nuevos (HD-039…HD-046): uno de correctitud de consulta con pérdida silenciosa de filas, dos de
seguridad (expiración de token no exigida y guarda de aislamiento *fail-open*), dos de observabilidad
(métricas contadas dentro de la transacción reintentable y fallos de plugin sin registrar), uno de
contrato de error, uno de arquitectura de lectura y uno de duplicación de la regla de autorización.
Verificación ejecutada el 2026-07-30: `typecheck` ✓, `lint` ✓, `format:check` ✓, `build` ✓,
`test` 188/188 ✓ (179 antes, +9 nuevas), `quality:all` 18/18 ✓ sin deriva de contrato
(`quality:openapi` 47 rutas, `quality:routes` 49 operaciones). La ejecutabilidad del SQL de
`AgentRunQueryRepository` sobre el rol reader y el comportamiento de HD-039 contra datos con empates
reales quedan pendientes de `yarn test:integration` sobre base desechable: pendiente de entorno, no
de código.
